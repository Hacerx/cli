# hacerx

> A CLI tool for day-to-day tasks.

## Installation

```bash
npm install -g hacerx
```

## Usage

```bash
hacerx <command> [options]
```

## Commands

| Command | Description | Details |
|---------|-------------|---------|
| `sf extract test` | Utility to find test files in the base directory and its subdirectories. | [docs](docs/sf.md#sf-extract-test) |
| `sf permissionset generate-admin` | Generate a permissionset for system admins with full permissions to all objects | [docs](docs/sf.md#sf-permissionset-generate-admin) |
| `sf types object` | SObject(s) to get types for | [docs](docs/sf.md#sf-types-object) |

---
*This documentation was auto-generated. Run `npm run docs` to regenerate.*
