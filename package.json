{
  "name": "hacerx",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "build:watch": "tsc -w",
    "build": "tsc",
    "build:scripts": "tsc -p tsconfig.scripts.json",
    "docs": "npm run build && npm run build:scripts && node scripts/generate-docs.js",
    "build:pro": "npm run build:scripts && node scripts/build-pro.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "devDependencies": {
    "@types/node": "^22.15.21",
    "@vitest/coverage-v8": "^4.1.2",
    "esbuild": "^0.27.5",
    "typescript": "^5.8.3",
    "vitest": "^4.1.2"
  },
  "dependencies": {
    "@hacerx/sf-auth-decrypt": "^1.1.4",
    "@jsforce/jsforce-node": "^3.10.14",
    "commander": "^14.0.0",
    "prettier": "^3.5.3"
  },
  "bin": {
    "hacerx": "./dist/index.js"
  }
}
